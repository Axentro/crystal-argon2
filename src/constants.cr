module Argon2
   DEFAULT_SALT_LEN = 16 # DEFAULT_SALT_LEN is a standard recommendation from the Argon2 spec.
   OUT_LEN = 32          # Binary, unencoded output
   ENCODE_LEN = 108      # Encoded output
end
